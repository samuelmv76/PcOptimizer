<Application x:Class="PcOptimizer.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
<<<<<<< HEAD
        <ResourceDictionary>
            <!--
                La paleta de colores (Themes/Dark.xaml o Themes/Light.xaml) la
                inserta ThemeManager al arrancar, delante de esta lista, segun
                la preferencia guardada. Aqui solo van formas y comportamiento.
            -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Controls.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
=======

        <!-- Paleta. Un solo sitio donde cambiar los colores. -->
        <Color x:Key="AccentColor">#2F6FEB</Color>
        <SolidColorBrush x:Key="Accent" Color="{StaticResource AccentColor}" />
        <SolidColorBrush x:Key="SidebarBackground" Color="#1F2430" />
        <SolidColorBrush x:Key="SidebarForeground" Color="#C9CEDA" />
        <SolidColorBrush x:Key="SidebarSelected" Color="#2C3446" />
        <SolidColorBrush x:Key="PageBackground" Color="#FFFFFF" />
        <SolidColorBrush x:Key="PanelBackground" Color="#F5F6F8" />
        <SolidColorBrush x:Key="Hairline" Color="#E2E5EA" />
        <SolidColorBrush x:Key="MutedText" Color="#5C6472" />
        <SolidColorBrush x:Key="WarningBackground" Color="#FFF6E5" />
        <SolidColorBrush x:Key="WarningBorder" Color="#E0A33B" />

        <Style TargetType="Button">
            <Setter Property="Padding" Value="14,6" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="MinWidth" Value="92" />
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource Accent}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <Style x:Key="LinkButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource Accent}" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="PageTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="19" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="Muted" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedText}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

>>>>>>> 77a6a47fbf3cb9b7c8cc565933bd42c37265aab0
    </Application.Resources>
</Application>
